//Arrays

const names = ["reinaldo", "angela", "masgo"];

// push -> add, last position
// pop -> remove, last position

// shift -> add, first position
// unshift -> remove, first position

//all these functions that remove an element from the array, return the removed element

names.push("test");

console.log(names);
